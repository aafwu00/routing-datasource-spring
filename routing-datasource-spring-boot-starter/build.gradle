dependencies {
    compile project(':routing-datasource-spring-boot-autoconfigure')

    compile 'org.springframework.boot:spring-boot-starter-jdbc'
    compile 'org.springframework.data:spring-data-commons'
}
